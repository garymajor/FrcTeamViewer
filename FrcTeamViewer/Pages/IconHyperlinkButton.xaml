<UserControl
    x:Class="FrcTeamViewer.Pages.IconHyperlinkButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:app="using:FrcTeamViewer"
    xmlns:local="using:FrcTeamViewer.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="35"
    d:DesignWidth="200">

    <Grid x:Name="LayoutRoot" >
        <HyperlinkButton x:Name="iconHyperlinkButton" Command="{Binding Command}" CommandParameter="{Binding CommandParameter}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding IconContent}" FontFamily="Segoe MDL2 Assets" FontSize="20" />
                <TextBlock Text="{Binding ButtonContent}" Margin="3,0,0,0" />
            </StackPanel>
        </HyperlinkButton>
    </Grid>
</UserControl>
